{
  "name": "node-gyp-swig-experiment",
  "private": true,
  "version": "0.0.1",
  "description": "Helloworld of swig to nodeJS",
  "main": "",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "swig -javascript -node -c++ example.i && node-gyp build"
  }
}
